import {SelectableTableDirective} from './selectable-table.directive';

describe('SelectabledTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableTableDirective();
    expect(directive).toBeTruthy();
  });
});
